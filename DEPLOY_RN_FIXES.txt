RUFFNECK FIXES v2 — READY TO DEPLOY
===================================

Deep audit results + what this package fixes:

1. LANGUAGE ON HOMEPAGE
   - index.html was missing the language button HTML (Blog/Store already had it)
   - Fix injects 🌐 EN button + Google Translate on Homepage only if missing

2. BLOG SEARCH
   - Two search inputs existed (rnBlogSearch + searchInput) but only one was wired
   - filterBlog only read searchInput → rnBlogSearch did nothing
   - Fix syncs both inputs and runs one unified filter

3. BLOG CATEGORY FILTERS
   - Mismatch: buttons used "operations" / cards used "ops"; "career" vs "careers"
   - CMS cards use mapped cats (ops, career, ai…)
   - Fix uses soft aliases so both match

4. STORE SEARCH + FILTERS
   - Already mostly correct; strengthened event binding + fallback

5. CURRENCY (₦ / $)
   - setCurrency existed; strengthened binding + price update from data-ngn
   - Does NOT change Flutterwave payment logic — only display + currency value passed if you already use currentCurrency

6. UI polish
   - Light hover only; no layout rewrite

NOT CHANGED
- Flutterwave checkout / product IDs / PRODUCTS object
- Google Apps Script
- /api/* and admin
- forms.js, cms.js core, app.js

DEPLOY
1. Unzip this package
2. Upload to Vercel (or git push)
3. Hard refresh (Ctrl+Shift+R)

TEST
- Homepage: language button top-right
- Blog: type in either search box + click categories
- Store: search + category pills + ₦/$ toggle
- Store: test purchase still works

Optional: change rate in js/rn-fixes.js → var RATE = 1600;
