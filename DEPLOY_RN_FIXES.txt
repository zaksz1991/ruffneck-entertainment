RUFFNECK FIXES v3 — READY TO DEPLOY
===================================

What was wrong (deep audit):
1. TWO search bars on Blog and Store (hardcoded in HTML)
   - Store: #rnStoreSearch + #searchInput
   - Blog:  #rnBlogSearch + #searchInput
2. Store could hang waiting on /api/cms/products (returns empty products)
3. Blog category mismatches (ops vs operations, etc.)
4. Homepage language button was missing markup (fixed earlier)

What v3 does:
- Hides the extra search bar on Blog + Store (one search only)
- CMS product fetch has 4-second timeout so Store never stays loading forever
- Search/filters/currency/language still fixed as before
- Flutterwave / APIs / Google Apps Script / admin NOT touched

DEPLOY:
1. Unzip
2. Upload entire folder to Vercel (or git push)
3. Hard refresh Ctrl+Shift+R

TEST:
- Blog: only ONE search box visible; typing filters articles
- Store: only ONE search box; page loads products (not stuck)
- Store: ₦/$ toggle works
- Homepage: 🌐 language button
- Purchase still works
