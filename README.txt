Store polish pass (user CSS + a11y + filter guard)
=====================================================
Files: store.html only
Preserved: product IDs, prices, tiers, Flutterwave, globals
Added: rn-store-polish-pass CSS, checkout reassurance wording,
       filter bind guard calling existing filterProducts,
       modal/cart aria+scroll lock adapter
