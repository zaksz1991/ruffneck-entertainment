CRITICAL DEPLOY INSTRUCTIONS

The live site was NOT receiving previous store.html updates
(rn-store-commercial-ui was missing from production).

1. Download store.html from this ZIP
2. In GitHub: open your repo → find store.html in the ROOT
3. Click pencil (edit) OR upload and REPLACE store.html
4. Commit to main (or the branch Vercel Production uses)
5. Vercel → Deployments → wait until Ready (green)
6. Open PRIVATE/INCOGNITO:
   https://ruffneck-entertainment.vercel.app/store?v=loadfix30
7. Scroll DOWN past the dark hero

If still stuck: View Page Source on phone/desktop and search for:
   rn-store-safe-init
If that text is missing, the new file is NOT on production yet.
